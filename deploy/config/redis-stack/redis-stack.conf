# 连接密码
requirepass "XmSX6jCpHNB5tsmw8QXn"

# 开启持久化(AOF)
appendonly "yes"

# 持久化存储文件名称
appendfilename "appendonly.aof"

# 指定数据目录
dir /data

# 以下是加载Redis的模块，根据需求添加
loadmodule /opt/redis-stack/lib/redisearch.so
loadmodule /opt/redis-stack/lib/rejson.so